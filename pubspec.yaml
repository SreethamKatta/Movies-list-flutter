name: flutterstackeditemlist
description: A new Flutter application.


publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  firebase_core: ^1.4.0
  firebase_auth: ^3.0.1
  firebase_analytics: ^8.2.0
  shared_preferences: ^0.5.6+1
  google_sign_in: ^5.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:


  uses-material-design: true


  assets:
    - assets/images/
